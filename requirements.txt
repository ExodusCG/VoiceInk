# VoiceInk - 核心依赖
sounddevice>=0.4.6
numpy>=1.24
pyyaml>=6.0

# ASR 引擎 - SenseVoice (推荐，多语言，无需 VC++)
sherpa-onnx>=1.12.0

# 可选 ASR 引擎
# pywhispercpp>=0.2.0      # Whisper.cpp
# faster-whisper>=1.0.0    # Faster Whisper (CTranslate2)

# LLM 推理
llama-cpp-python>=0.2.80
openai>=1.0                  # 云端 API 客户端 (可选)

# UI & 系统交互
pystray>=0.19
Pillow>=10.0
pyperclip>=1.8

# 工具
pypinyin>=0.49

# VAD
webrtcvad>=2.0.10
